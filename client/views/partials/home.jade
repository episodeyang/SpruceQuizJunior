include mixins/navbar
include mixins/footer
div.background(background-img="/img/collections.jpg")
#cover(data-ng-hide="appInitialized", data-ng-animate="{ hide: 'fade-hide' }")
.container
    +navbar
    .row(style="padding-top:100px;")
    .row
        .hidden-xs.col-sm-3.col-md-2(style="text-align:center;")
            img.img-circle(ph-img="130x130", style="margin-bottom:20px;margin-left:-15px;").border-circle
            h3 添加错题&nbsp
                span.glyphicon.glyphicon-plus
            h3 杉景题库&nbsp
                span.glyphicon.glyphicon-share-alt
        .col-sm-9.col-md-10
            .row(style="margin-top:50px;")
                #panel-1(ng-class="viewCtrl.panelState", ng-click="viewCtrl.panelState='s1'")
                    h1 错题本
                        //-span.glyphicon.glyphicon-chevron-left
                .panel-content(ng-show="viewCtrl.panelState=='s1'")
                    //-(ng-class="viewCtrl.panelState")
                    .row
                        .col-md-10(style="padding-top:140px;")
                            h2(ng-repeat-start="erratum in model.errata")
                                a(ng-href="{{'/errata/'+erratum._id}}") {{erratum.title}}
                            a.close-button(ng-repeat-end, ng-show="viewCtrl.state == 'editView' ",
                            ng-click="delete(erratum._id)")
                                span.glyphicon.glyphicon-remove
                            h2 {{title}}
                            .row(ng-show="viewCtrl.state == 'createView' ", style="margin-top:40px;")
                                .col-xs-offset-1.col-xs-10
                                    form(role='form').form-horizontal
                                        .form-group
                                            label.col-xs-4.control-label 学科
                                            .col-xs-8
                                                input#input1.form-control(type='subject', placeholder='',ng-model="subject")
                                        .form-group
                                            label.col-xs-4.control-label 错题本标题
                                            .col-sm-8
                                                input#input2.form-control(type='title', placeholder='{{subject}}',ng-model="title")
                                        button.btn.btn-default(ng-click="viewCtrl.state =  'defaultView' ").col-sm-2.col-sm-offset-7 取消
                                        button.btn.btn-default(type='submit', ng-click="createErratum()").col-sm-offset-1.col-sm-2 完成
                            .row(ng-show="viewCtrl.state == 'defaultView' ")
                                .col-xs-12
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'createView' ")
                                        span.glyphicon.glyphicon-plus
                                        p 添加
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'editView' ")
                                        span.glyphicon.glyphicon-remove-sign
                                        p 编辑
                            .row(ng-show="viewCtrl.state == 'editView' ")
                                .col-xs-12
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'defaultView' ")
                                        span.glyphicon.glyphicon-share-alt
                                        p 完成编辑
                #panel-2(ng-class="viewCtrl.panelState", ng-click="viewCtrl.panelState='s2'")
                    h1 最近&nbsp
                .panel-content(ng-show="viewCtrl.panelState=='s2'")
                    //-(ng-class="viewCtrl.panelState")
                    .row
                        .col-md-10(style="padding-top:140px;")
                            h2(ng-repeat-start="erratum in model.errata")
                                a(ng-href="{{'/errata/'+erratum._id}}") {{erratum.title}}
                            a.close-button(ng-repeat-end, ng-show="viewCtrl.state == 'editView' ",
                            ng-click="delete(erratum._id)")
                                span.glyphicon.glyphicon-remove
                            h2 {{title}}
                            .row(ng-show="viewCtrl.state == 'createView' ", style="margin-top:40px;")
                                .col-xs-offset-1.col-xs-10
                                    form(role='form').form-horizontal
                                        .form-group
                                            label.col-xs-4.control-label 学科
                                            .col-xs-8
                                                input#input1.form-control(type='subject', placeholder='',ng-model="subject")
                                        .form-group
                                            label.col-xs-4.control-label 错题本标题
                                            .col-sm-8
                                                input#input2.form-control(type='title', placeholder='{{subject}}',ng-model="title")
                                        button.btn.btn-default(ng-click="viewCtrl.state =  'defaultView' ").col-sm-2.col-sm-offset-7 取消
                                        button.btn.btn-default(type='submit', ng-click="createErratum()").col-sm-offset-1.col-sm-2 完成
                            .row(ng-show="viewCtrl.state == 'defaultView' ")
                                .col-xs-12
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'createView' ")
                                        span.glyphicon.glyphicon-plus
                                        p 添加
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'editView' ")
                                        span.glyphicon.glyphicon-remove-sign
                                        p 编辑
                            .row(ng-show="viewCtrl.state == 'editView' ")
                                .col-xs-12
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'defaultView' ")
                                        span.glyphicon.glyphicon-share-alt
                                        p 完成编辑
                #panel-3(ng-class="viewCtrl.panelState", ng-click="viewCtrl.panelState='s3'")
                    h1 复习&nbsp
                        //-span.glyphicon.glyphicon-chevron-right
                .panel-content(ng-show="viewCtrl.panelState=='s3'")
                    //-(ng-class="viewCtrl.panelState")
                    .row
                        .col-md-10(style="padding-top:140px;")
                            h2(ng-repeat-start="erratum in model.errata")
                                a(ng-href="{{'/errata/'+erratum._id}}") {{erratum.title}}
                            a.close-button(ng-repeat-end, ng-show="viewCtrl.state == 'editView' ",
                            ng-click="delete(erratum._id)")
                                span.glyphicon.glyphicon-remove
                            h2 {{title}}
                            .row(ng-show="viewCtrl.state == 'createView' ", style="margin-top:40px;")
                                .col-xs-offset-1.col-xs-10
                                    form(role='form').form-horizontal
                                        .form-group
                                            label.col-xs-4.control-label 学科
                                            .col-xs-8
                                                input#input1.form-control(type='subject', placeholder='',ng-model="subject")
                                        .form-group
                                            label.col-xs-4.control-label 错题本标题
                                            .col-sm-8
                                                input#input2.form-control(type='title', placeholder='{{subject}}',ng-model="title")
                                        button.btn.btn-default(ng-click="viewCtrl.state =  'defaultView' ").col-sm-2.col-sm-offset-7 取消
                                        button.btn.btn-default(type='submit', ng-click="createErratum()").col-sm-offset-1.col-sm-2 完成
                            .row(ng-show="viewCtrl.state == 'defaultView' ")
                                .col-xs-12
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'createView' ")
                                        span.glyphicon.glyphicon-plus
                                        p 添加
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'editView' ")
                                        span.glyphicon.glyphicon-remove-sign
                                        p 编辑
                            .row(ng-show="viewCtrl.state == 'editView' ")
                                .col-xs-12
                                    h3.h3-verticle(ng-click="viewCtrl.state = 'defaultView' ")
                                        span.glyphicon.glyphicon-share-alt
                                        p 完成编辑

link(href='/css/homePage.css', rel="stylesheet")
