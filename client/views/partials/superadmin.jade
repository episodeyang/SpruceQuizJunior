include mixins/navbar
include mixins/footer
div.background
#cover(data-ng-hide="appInitialized", data-ng-animate="{ hide: 'fade-hide' }")
.sp-container
    +navbar
    .container(style="width:900px;")
        .panel-left
            //-p(ph-txt='5p2s')
        .panel-body
            .container
                .row
                    tabset
                        tab(heading="用户")
                            div.tab-box.background-white
                                .container
                                    //div(ng-controller="SuperAdminUserTableCtrl")
                                    br
                                    button.btn.btn-success(ng-click="listStudents()") 学生数据
                                    br
                                    br
                                    h3(ph-txt='1s')
                                    div(ph-txt='3p2s').background-white
                                    div(ng-show="studentShowClicked && data['student'].data.length > 0")
                                        //dir-test(pageStatus="pageStatus")
                                        //json-table(type="student", data="data", pageStatus="pageStatus")

                        tab(heading="班级")
                            p TODO
                            //button.btn.btn-success(ng-click="listSections()") 班级数据
                            //p {{model.sectionList}}
                        tab(heading="教学材料")
        .panel-right
            div(style="width:80px;margin:20px;float:left;")
                div.img-circle.border-white(ph-img='78x78', style='width: 82px; height: 82px; background-repeat: no-repeat;')
            div(style="width:110px;margin-top:20px; float:left;")
                div.user-bio
                    h4 {{userData.lastName + userData.firstName}}
                    p
                        small {{userData.schools[0].schoolName}}
                        br
                        small {{userData.roleTitle}}
            .container
        +footer




